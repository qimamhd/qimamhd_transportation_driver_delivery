# v13.0.4.5.1 - Period visibility

- Shows company explicitly in delivery period list/form.
- Adds app-driver count, allowed-to-register count, blocked-batch count, and existing batch count.
- Adds smart buttons for all period batches and blocked batches.
- Does not reopen driver batches automatically.
- Does not change delivery, review, settlement, rollback, GPS, UUID, or API contracts.
- Counts app-enabled employees marked as drivers in the same company.
